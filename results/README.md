# Results

This folder stores results separated into raw and processed forms.

- `raw/`: original outputs
- `processed/`: cleaned or transformed outputs
- `tables/`: table artifacts
- `figures/`: plots and images

Guidelines:

- Keep reproducible source outputs in `raw/`.
- Build shareable tables and figures here, then promote final materials into `publish/` or `reports/`.
