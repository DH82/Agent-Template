# Logs

This folder stores logs shared across experiments.

- `system/`: environment and system-level logs
- `batches/`: logs from grouped runs
- `debug/`: shared debugging logs

Guidelines:

- Put experiment-specific logs inside the experiment folder first.
- Include dates or run identifiers in log file names.
