# Experiment Area Rules

- Keep the status-based folder structure under `experiments/`.
- Prefer `scripts/expctl.py init ...` when creating a new experiment.
- Keep key experimental conclusions in the experiment `README.md`.
- Store logs and results in the experiment folder first, then promote shared outputs when needed.
- When status changes, update both folder location and `experiment.json`.
