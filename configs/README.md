# Configs

This folder stores configuration shared across experiments.

- `baselines/`: baseline configs
- `variants/`: changed configs
- `shared/`: config fragments reused across setups

Guidelines:

- Keep only shared configs here.
- Put one-off configs inside the relevant experiment folder.
- Prefer file names that make the purpose obvious.
