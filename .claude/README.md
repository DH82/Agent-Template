# Claude Project Files

This folder stores Claude Code project-level helper files.

- `agents/`: reusable project subagents

Keep shared always-on rules in the root `CLAUDE.md` and `AGENTS.md`.
Use subagents only for specialized reusable workflows.
