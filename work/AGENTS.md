# Work Area Rules

- Read `TASK.md`, `CONTEXT.md`, and `PLAN.md` before changing an existing work item.
- Record important decision changes in `DECISIONS.md`.
- Record progress in `LOG.md`.
- Save important outputs in `OUTPUT/` and disposable material in `SCRATCH/`.
- When task status changes, keep folder location and `task.json` aligned.
