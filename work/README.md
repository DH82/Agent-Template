# Work

This folder stores generic work by status.

- `inbox/`: not started yet
- `active/`: in progress
- `blocked/`: blocked by external reasons
- `completed/`: finished
- `archive/`: long-term storage

Prefer creating work items with `scripts/workctl.py init ...`.
